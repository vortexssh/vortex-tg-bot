services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    # Docker bridge DNS is broken on this VPS (Errno -3 / Network unreachable).
    # Host network uses the same stack as curl on the machine.
    network_mode: host
